# Schemas

Generate the deterministic topology schema with:

```bash
w2p schema --out schemas/topology.schema.json
```

The API also serves the current contracts from:

- `GET /v1/topology/schema`
- `GET /v1/diagram/schema`

